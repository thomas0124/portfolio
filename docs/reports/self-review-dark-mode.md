# Self-Review: dark-mode

- Date: 2026-08-25
- Reviewer: reviewer subagent
- Verdict: PASS

## Findings

所見なし（CRITICAL な問題は検出されませんでした）。

以下、参考として確認済み事項と非ブロッキングの観察を記載します。

### 確認済み（問題なし）

- **不要な変更**: なし。diff は `tailwind.config.js`（`darkMode: 'media'` 1行追加）、`app/globals.css`（`@media (prefers-color-scheme: dark)` ブロック追加）、プランファイル（新規）のみ。フォーマットのみの変更や無関係な巻き込みなし。
- **命名**: CSS カスタムプロパティ名は既存の `:root`（ライト）と完全に一致。shadcn/ui の HSL トークン体系を踏襲しており grep 可能で一貫している。
- **可読性**: ダークブロックはライト `:root` と同じ変数順序・同じフォーマットで並んでおり、対応関係が目視で追える。ネストは `@layer base > @media > :root` の 3 段で妥当。
- **変数カバレッジ**: ライト `:root` の全カラー変数 19 個（`--background` 〜 `--ring`）がダークブロックで過不足なく上書きされている。`--radius`（レイアウト値、非カラー）は意図的に未上書きで正しい。
- **タイポ / コピペ誤り**: なし。HSL 値のフォーマット（`H S% L%`）は全行で一貫。
- **デバッグコード / コメントアウト**: なし。
- **シークレット / 認証情報**: なし（純粋な CSS 変数と設定値のみ）。
- **セキュリティ**: 該当なし。インジェクション・XSS 等のリスク面はなし（静的 CSS/設定）。
- **例外処理**: 該当なし。

### 非ブロッキングの観察（follow-up、マージを妨げない）

- `--accent`（`193 38% 63%`）、`--accent-foreground`（`0 0% 100%`）、`--ring`（`193 38% 63%`）はライトと同値のままダークブロックにも再掲されている。これは冗長だが、変数セットを完全に列挙してライトとの対応を明示する意図として妥当。将来カラー調整時に一箇所で完結できる利点もあり、削除は必須ではない。プランの Non-goals（カラーパレットの大幅再設計はしない）とも整合。
- `--destructive` はライト `0 84.2% 60.2%` に対しダーク `0 70% 50%` と明度/彩度を下げており、暗背景でのコントラスト調整として妥当な意図が読み取れる。

## Summary

diff は OS 設定連動ダークモードのための最小限かつ焦点の絞れた変更で、命名・可読性・カバレッジともにライトテーマと一貫しており、シークレット漏洩・デバッグコード・不要な変更・セキュリティ上の懸念はいずれも検出されなかった。CRITICAL な所見はなく PASS。
